apiVersion: v1
kind: Secret
metadata:
  name: model-platform-secret
  namespace: model-platform
type: Opaque
stringData:
  # 数据库密码
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  DATABASE_URL: "postgresql://admin:CHANGE_ME_IN_PRODUCTION@postgres:5432/model_platform"

  # Redis 密码
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

  # JWT 密钥
  SECRET_KEY: "CHANGE_ME_IN_PRODUCTION_USE_RANDOM_STRING"

  # 对象存储密钥
  S3_ACCESS_KEY: "CHANGE_ME_IN_PRODUCTION"
  S3_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"

  # 第三方服务密钥
  OPENAI_API_KEY: ""
  HUGGINGFACE_TOKEN: ""
